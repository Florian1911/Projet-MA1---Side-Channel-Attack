#!/usr/bin/env python3
import argparse
import numpy as np
import matplotlib.pyplot as plt

SBOX = np.array([
    
], dtype=np.uint8)
HW = np.array([bin(i).count('1') for i in range(256)], dtype=np.float64)


def preprocess(tr):
    t = tr.astype(np.float64, copy=False)
    t = t - t.mean(axis=1, keepdims=True)
    x = np.linspace(-1.0, 1.0, t.shape[1], dtype=np.float64)
    t = t - np.outer((t @ x) / np.dot(x, x), x)
    return t


def cpa_curve(tr, pbyte, key):
    tc = tr - tr.mean(axis=0, keepdims=True)
    tstd = tr.std(axis=0, ddof=1) + 1e-15
    h = HW[SBOX[np.bitwise_xor(pbyte, key)]]
    hc = h - h.mean()
    hstd = h.std(ddof=1) + 1e-15
    corr = (hc @ tc) / ((tr.shape[0] - 1) * hstd * tstd)
    return corr


def cpa_scores_at_poi(y, pbyte):
    yc = y - y.mean()
    ystd = y.std(ddof=1) + 1e-15
    scores = np.zeros(256, dtype=np.float64)
    for k in range(256):
        h = HW[SBOX[np.bitwise_xor(pbyte, k)]]
        hc = h - h.mean()
        hstd = h.std(ddof=1) + 1e-15
        scores[k] = abs((hc @ yc) / ((len(y) - 1) * hstd * ystd))
    return scores


def main():
    ap = argparse.ArgumentParser(description='AES-step byte0 CPA POI analysis')
    ap.add_argument('--npz', required=True)
    ap.add_argument('--byte', type=int, default=0)
    ap.add_argument('--true-key', type=lambda x: int(x, 0), default=0x2b)
    ap.add_argument('--win-start', type=int, default=100)
    ap.add_argument('--win-end', type=int, default=260)
    ap.add_argument('--n-traces', type=int, default=5000)
    ap.add_argument('--out', default='aes_step_byte0_analysis.png')
    args = ap.parse_args()

    d = np.load(args.npz)
    tr = d['traces'][:args.n_traces, args.win_start:args.win_end]
    p = d['plaintexts'][:args.n_traces, args.byte].astype(np.uint8)

    tr = preprocess(tr)

    corr_true = cpa_curve(tr, p, args.true_key)
    poi = int(np.argmax(np.abs(corr_true)))

    y = tr[:, poi]
    scores = cpa_scores_at_poi(y, p)
    order = np.argsort(scores)[::-1]
    rank = int(np.where(order == args.true_key)[0][0]) + 1

    print(f'POI global: {args.win_start + poi}')
    print(f'true key=0x{args.true_key:02x} rank={rank} score={scores[args.true_key]:.6f}')
    print('top5:', ' '.join(f'0x{int(k):02x}:{scores[k]:.6f}' for k in order[:5]))

    fig, ax = plt.subplots(2, 1, figsize=(10, 7))
    ax[0].plot(corr_true, lw=1.0)
    ax[0].axvline(poi, color='r', ls='--', lw=0.8)
    ax[0].set_title(f'CPA corr curve for true key 0x{args.true_key:02x}')
    ax[0].set_ylabel('corr')

    ax[1].plot(scores, lw=0.8)
    ax[1].axvline(args.true_key, color='g', ls='--', lw=0.8)
    ax[1].set_title(f'Key scores at POI={args.win_start + poi} (rank={rank})')
    ax[1].set_xlabel('key guess')
    ax[1].set_ylabel('|corr|')

    plt.tight_layout()
    fig.savefig(args.out, dpi=140)
    print(f'saved: {args.out}')


if __name__ == '__main__':
    main()
